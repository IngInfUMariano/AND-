info:
  name: Listar mis direcciones
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}api/mis-direcciones"
  auth:
    type: bearer
    token: "{{token_tienda}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
