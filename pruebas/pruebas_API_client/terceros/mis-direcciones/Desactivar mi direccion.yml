info:
  name: Desactivar mi direccion
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}api/mis-direcciones/1"
  auth:
    type: bearer
    token: "{{token_tienda}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
