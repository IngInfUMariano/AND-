info:
  name: Desasociar producto del proveedor
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}api/proveedores/1/productos/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
