info:
  name: Listar proveedores
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}api/proveedores"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
