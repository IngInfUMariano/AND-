info:
  name: Historial de entradas proveedor
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}api/proveedores/1/historial"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
