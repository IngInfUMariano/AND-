info:
  name: Listar productos del proveedor
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}api/proveedores/1/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
