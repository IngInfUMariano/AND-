info:
  name: Desactivar proveedor
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{base_url}}api/proveedores/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
