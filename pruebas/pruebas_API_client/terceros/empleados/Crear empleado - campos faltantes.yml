info:
  name: Crear empleado - campos faltantes
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}api/empleados"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nombres": "Sin Datos"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
