info:
  name: Cambiar sucursal empleado
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base_url}}api/empleados/3/sucursal"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sucursal_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
