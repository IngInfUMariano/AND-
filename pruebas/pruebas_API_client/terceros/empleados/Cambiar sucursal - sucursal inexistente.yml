info:
  name: Cambiar sucursal - sucursal inexistente
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}api/empleados/3/sucursal"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sucursal_id": 9999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
