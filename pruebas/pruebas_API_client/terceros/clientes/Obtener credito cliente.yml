info:
  name: Obtener credito cliente
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}api/clientes/2/credito"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
