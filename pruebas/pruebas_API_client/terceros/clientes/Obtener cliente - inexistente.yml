info:
  name: Obtener cliente - inexistente
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}api/clientes/9999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
