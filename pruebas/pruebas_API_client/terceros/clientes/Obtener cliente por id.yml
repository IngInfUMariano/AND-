info:
  name: Obtener cliente por id
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}api/clientes/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
