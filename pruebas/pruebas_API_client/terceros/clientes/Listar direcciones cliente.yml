info:
  name: Listar direcciones cliente
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}api/clientes/1/direcciones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
