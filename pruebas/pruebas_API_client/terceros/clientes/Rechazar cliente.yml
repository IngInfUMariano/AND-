info:
  name: Rechazar cliente
  type: http
  seq: 11

http:
  method: POST
  url: "{{base_url}}api/clientes/6/rechazar"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "motivo": "Documentación incompleta, NIT no verificado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
