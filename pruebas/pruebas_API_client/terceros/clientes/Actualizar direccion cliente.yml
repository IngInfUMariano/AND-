info:
  name: Actualizar direccion cliente
  type: http
  seq: 17

http:
  method: PUT
  url: "{{base_url}}api/clientes/1/direcciones/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "alias": "Casa Principal",
        "telefono": "55009911"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
