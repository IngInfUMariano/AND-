info:
  name: Listar clientes pendientes
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}api/clientes/pendientes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
