info:
  name: Listar clientes
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}api/clientes"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
