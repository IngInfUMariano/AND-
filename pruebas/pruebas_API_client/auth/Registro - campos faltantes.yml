info:
  name: Registro - campos faltantes
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}api/auth/registro"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "incompleto@gmail.com"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: https://and-2yw6.onrender.com/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
