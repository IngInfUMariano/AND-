info:
  name: Obtener perfil - sin token
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}api/auth/perfil"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
