info:
  name: Recuperar - email invalido
  type: http
  seq: 11

http:
  method: POST
  url: "{{base_url}}api/auth/recuperar"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "esto-no-es-un-email"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: https://and-2yw6.onrender.com/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
