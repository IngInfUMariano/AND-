info:
  name: LoginCliente
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/auth/tienda"
  body:
    type: json
    data: |-
      {
        "email": "ana.morales@gmail.com",
        "password": "Cliente123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
