info:
  name: Login interno - campo faltante
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}api/auth/interno"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@inventa.gt"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: https://and-2yw6.onrender.com/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
