info:
  name: Cambiar password - nueva muy corta
  type: http
  seq: 18

http:
  method: PUT
  url: "{{base_url}}api/auth/password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "password_actual": "Admin123!",
        "password_nuevo": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
