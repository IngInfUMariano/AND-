info:
  name: LoginInterno
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/interno"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@inventa.gt",
        "password": "Admin123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
