info:
  name: Restablecer - token invalido
  type: http
  seq: 13

http:
  method: POST
  url: "{{base_url}}api/auth/restablecer"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "token": "tokeninvalidooexpirado1234567890abcdef",
        "password": "NuevoAdmin123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
