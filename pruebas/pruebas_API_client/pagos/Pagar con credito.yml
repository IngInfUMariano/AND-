info:
  name: Pagar con credito
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}api/pagos/credito/1"
  auth:
    type: bearer
    token: "{{token_tienda}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
