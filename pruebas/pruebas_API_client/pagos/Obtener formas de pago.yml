info:
  name: Obtener formas de pago
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}api/pagos/formas-pago/1"
  auth:
    type: bearer
    token: "{{token_tienda}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
