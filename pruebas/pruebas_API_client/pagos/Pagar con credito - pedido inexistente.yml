info:
  name: Pagar con credito - pedido inexistente
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}api/pagos/credito/9999"
  auth:
    type: bearer
    token: "{{token_tienda}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
