info:
  name: Estado de cuenta - cliente inexistente
  type: http
  seq: 20

http:
  method: GET
  url: "{{base_url}}api/clientes/9999/estado-cuenta"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
