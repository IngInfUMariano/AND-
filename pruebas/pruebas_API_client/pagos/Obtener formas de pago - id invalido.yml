info:
  name: Obtener formas de pago - id invalido
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}api/pagos/formas-pago/abc"
  auth:
    type: bearer
    token: "{{token_tienda}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
