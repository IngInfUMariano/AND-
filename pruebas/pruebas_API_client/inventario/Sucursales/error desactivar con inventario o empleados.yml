info:
  name: Error desactivar sucursal con inventario o personal
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/sucursales/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5