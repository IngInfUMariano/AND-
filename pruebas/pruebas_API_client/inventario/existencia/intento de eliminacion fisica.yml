info:
  name: Error intento de eliminación física de stock
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/existencias/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5