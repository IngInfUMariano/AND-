info:
  name: obtener existencia invaida
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/existencias/9999"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
