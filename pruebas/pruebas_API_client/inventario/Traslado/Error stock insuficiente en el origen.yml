info:
  name: Error stock insuficiente en origen
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/traslados"
  body:
    type: json
    data: |-
      {
        "sucursal_origen_id": 1,
        "sucursal_destino_id": 2,
        "detalles": [
          {
            "variante_id": 1,
            "cantidad": 99999
          }
        ]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5