info:
  name: 07 - Ver formas de pago (incluye CREDITO)
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}api/pagos/formas-pago/22"
  auth:
    type: bearer
    token: "{{token_mayorista}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
