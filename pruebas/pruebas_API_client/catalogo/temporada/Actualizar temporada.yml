info:
  name: Actualizar temporada
  type: http
  seq: 1

http:
  method: PUT
  url: "{{base_url}}/temporadas/1"
  body:
    type: json
    data: |-
      {
        "descripcion": "Colección renovada Primavera - Verano"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
