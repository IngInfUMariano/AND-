info:
  name: Desactivar temporada
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/temporadas/5"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
