info:
  name: Combinacion nombre y anio duplicado
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/temporadas"
  body:
    type: json
    data: |-
      {
        "nombre": "Primavera - Verano",
        "anio": 2026,
        "descripcion": "Intento duplicado"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​