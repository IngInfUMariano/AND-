info:
  name: Listar marcas por id
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/marcas/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
