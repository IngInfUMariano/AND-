info:
  name: Nombre duplicado
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/marcas"
  body:
    type: json
    data: |-
      {
        "nombre":"nike",
        "descripcion": "calzado y ropa deportiva error por nombre"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
