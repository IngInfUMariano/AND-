info:
  name: Listar marcas por id error inexistente
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/marcas/99999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
