info:
  name: Desactivar producto sin variantes activas
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/productos/8"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
