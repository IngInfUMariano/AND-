info:
  name: Desactivar producto con variantes activas
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/productos/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
