info:
  name: Caducar precio
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/precios/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
