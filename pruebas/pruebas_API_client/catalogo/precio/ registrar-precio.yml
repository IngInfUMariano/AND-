info:
  name: Error de campos al Crear / registrar precio
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/precios"
  body:
    type: json
    data: |-
      {
        "variante_id": 2,
        "tipo": "MINORISTA",
        "monto": 299.99
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
