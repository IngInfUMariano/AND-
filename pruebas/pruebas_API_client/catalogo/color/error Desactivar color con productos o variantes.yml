info:
  name: error Desactivar color con productos o variantes
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/colores/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
