info:
  name: Codigo hexadecimal invalido
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/colores"
  body:
    type: json
    data: |-
      {
        "nombre": "Rojo Invalido",
        "codigo_hex": "ZZZZZZ"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
