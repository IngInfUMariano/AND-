info:
  name: Crear categoria hija
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/categorias
  body:
    type: json
    data: |-
      {
        "nombre": "Lentes de Verano",
        "descripcion": "Ropa de verano, lentes y accesorios",
        "categoria_padre_id": 3
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
