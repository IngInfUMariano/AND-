info:
  name: Listar categorias con Estructura jerarquica
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/categorias"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
