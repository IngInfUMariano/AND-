info:
  name: Listar variantse con filtros y relaciones
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/variantes/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
