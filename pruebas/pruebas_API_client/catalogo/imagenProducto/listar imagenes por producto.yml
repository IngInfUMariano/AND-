info:
  name: listar imagenes por producto
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/imagen-productos?producto_id=1"
  params:
    - name: producto_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
