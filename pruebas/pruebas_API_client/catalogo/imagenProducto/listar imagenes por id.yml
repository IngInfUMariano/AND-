info:
  name: listar imagenes por id
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/imagen-productos/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
