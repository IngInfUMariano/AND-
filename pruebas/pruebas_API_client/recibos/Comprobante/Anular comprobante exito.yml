info:
  name: Anular comprobante y revertir inventario
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/comprobantes/3/anular"
  body:
    type: json
    data: |-
      {
        "motivo_anulacion": "Error en digitacion de la cantidad recibida"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
