info:
  name: Obtener comprobante por ID con detalles
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/comprobantes/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
