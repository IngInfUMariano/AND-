info:
  name: Error tipo de documento invalido
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/correlativos"
  body:
    type: json
    data: |-
      {
        "sucursal_id": 1,
        "tipo_documento": "FACTURA_INVALIDA",
        "serie": "F001"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5