info:
  name: Obtener correlativo por ID
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/correlativos/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
