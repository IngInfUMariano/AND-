info:
  name: Error salida stock insuficiente
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/movimientos-inventario"
  body:
    type: json
    data: |-
      {
        "tipo": "SALIDA_VENTA",
        "cantidad": 99999,
        "variante_id": 1,
        "sucursal_id": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5