info:
  name: Obtener movimiento por ID
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/movimientos-inventario/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5