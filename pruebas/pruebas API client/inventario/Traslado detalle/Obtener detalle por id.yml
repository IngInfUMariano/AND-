info:
  name: Obtener detalle de traslado por ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/traslado-detalles/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5