info:
  name: Registrar recepcion de item conforme
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/traslado-detalles/5/recepcion"
  body:
    type: json
    data: |-
      {
        "cantidad_recibida": 5
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
