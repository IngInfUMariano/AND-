info:
  name: Obtener existencia por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/existencias/1"
  auth: 
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5