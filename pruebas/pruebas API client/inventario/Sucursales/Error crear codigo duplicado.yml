info:
  name: Error crear sucursal codigo duplicado
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/sucursales"
  body:
    type: json
    data: |-
      {
        "codigo": "SUC-NORTE",
        "nombre": "Otra Sucursal Norte"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5