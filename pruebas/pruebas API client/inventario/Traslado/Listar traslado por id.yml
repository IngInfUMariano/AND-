info:
  name: Obtener traslado por ID con detalle
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/traslados/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5s