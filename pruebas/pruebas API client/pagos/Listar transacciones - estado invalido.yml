info:
  name: Listar transacciones - estado invalido
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}api/pagos/transacciones?estado=INVALIDO"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
