info:
  name: Crear cliente - email faltante
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}api/clientes"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nombre": "Cliente Sin Email",
        "tipo": "MINORISTA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
