info:
  name: Actualizar cliente
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}api/clientes/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "telefono": "55009900",
        "contacto_nombre": "Ana Morales Actualizada"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
