info:
  name: Obtener empleado por id
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}api/empleados/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
