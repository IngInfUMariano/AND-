info:
  name: 09 - Ver pedido confirmado
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}api/pedidos/22"
  auth:
    type: bearer
    token: "{{token_mayorista}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
