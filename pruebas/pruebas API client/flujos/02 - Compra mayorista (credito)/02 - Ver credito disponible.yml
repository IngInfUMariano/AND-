info:
  name: 02 - Ver credito disponible
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}api/clientes/2/credito"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
