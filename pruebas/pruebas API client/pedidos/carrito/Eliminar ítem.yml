info:
  name: Eliminar ítem
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/api/carrito/items/20"
  auth:
    type: bearer
    token: "{{token_cliente}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
