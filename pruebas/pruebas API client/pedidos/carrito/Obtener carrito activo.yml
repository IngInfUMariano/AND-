info:
  name: Obtener carrito activo
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/carrito"
  headers:
    - name: Authorization
      value: Bearer {{token_cliente}}
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token_cliente}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
