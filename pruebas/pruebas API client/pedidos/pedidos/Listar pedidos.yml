info:
  name: Listar pedidos
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/pedidos"
  auth:
    type: bearer
    token: "{{token_cliente}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
