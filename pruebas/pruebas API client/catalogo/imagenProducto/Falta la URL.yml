info:
  name: Falta la URL
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/imagen-productos"
  body:
    type: json
    data: |-
      {
        "producto_id": 1,
        "orden": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
