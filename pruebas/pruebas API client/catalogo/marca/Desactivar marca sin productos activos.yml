info:
  name: Desactivar marca sin productos activos
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/marcas/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
