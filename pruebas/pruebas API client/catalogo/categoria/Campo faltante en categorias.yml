info:
  name: Campo faltante en categorias
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/categorias
  body:
    type: json
    data: |-
      {
        "descripcion": "Ropa de verano, lentes y accesorios"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
