info:
  name: Eliminar subcategoria
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{base_url}}/categorias/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
