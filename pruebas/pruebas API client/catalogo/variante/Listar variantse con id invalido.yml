info:
  name: Listar variantse con id invalido
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/variantes/99999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
